sudo: false

language: rust

rust:
  - stable
  - beta
  - nightly

addons:
  apt:
    packages:
      - libclang1-3.7

script:
  - cargo build --verbose --features clang_3_7
  - cargo test --verbose
